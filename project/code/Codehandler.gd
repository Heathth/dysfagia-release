extends Node3D



#func _ready():
	#Global.time_now = get_ticks_msec()
	#var time_elapsed = Global.time_now - Global.time_start
	#print(time_elapsed)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
