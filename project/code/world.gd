extends Node3D

@export var col:Color = Color(0.597, 0.755, 0.85)


# Called when the node enters the scene tree for the first time.
#func _ready():
	#await get_tree().process_frame
	#var materiall := ORMMaterial3D.new()
	#materiall.albedo_color = col
	#materiall.shading_mode = BaseMaterial3D.SHADING_MODE_UNSHADED
	#$BBack.set_surface_override_material(0, materiall)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
